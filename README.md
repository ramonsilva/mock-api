Projeto Exemplo para testes de rotas mockadas

no terminal na página do projeto, rodar:
- npm install
- npm start
- O endereço padrão que o servidor rodará será em: http://localhost:3000


As respostas mockadas estão dentro da pasta mocks.